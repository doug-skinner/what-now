pre-commit:
  parallel: true
  commands:
    biome-format:
      run: npx biome format "src/**/*.{ts,tsx,js,jsx,json,md}" --write
      stage_fixed: true

pre-push:
  parallel: false
  commands:
    biome-check:
      run: npx biome check
